import express from 'express';
import { InvoiceController } from './invoice.controller';

const router = express.Router();

// router.get('/', InvoiceController);

export const invoiceRoutes = router;
