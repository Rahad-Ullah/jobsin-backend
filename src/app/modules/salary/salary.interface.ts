export interface ISalaryComparison {
  keyword: string;
  category: string;
  subCategory: string;
  salaryAmount: number;
  city: string;
  state: string;
  country: string;
  averageSalary: number;
  minSalary: number;
  maxSalary: number;
}
